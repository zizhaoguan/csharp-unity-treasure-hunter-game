using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpecialBoard1 : MonoBehaviour
{
    // Start is called before the first frame update


    public void BreakSpecialBoard()
    {
        Destroy(this);
    }
}
